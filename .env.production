# Build de produção (npm run build) — consumido em src/App.jsx (const API_BASE).
# URL do backend serverless (Vercel), agora com domínio próprio em vez de *.vercel.app.
REACT_APP_API_URL=https://api.playnexusrpg.com

# ── O código-fonte NÃO vai para produção (spec 0036 · AC-8) ──────────────────
# Sem esta linha o CRA publica `main.*.js.map` junto do bundle, e o arquivo
# responde 200 em https://playnexusrpg.com — 6,5 MB com o React original,
# nomes de variável, comentários e a lógica de negócio inteira.
#
# Mora AQUI, e não no craco.config.js nem no CI, porque `.env.production` é
# lido em TODO `npm run build`: cobre o build local, o predeploy do Firebase
# (firebase.json:3), o GitHub Actions (ci.yml) e a Vercel de uma vez só.
GENERATE_SOURCEMAP=false

# ── Avisos de lint não derrubam o build ─────────────────────────────────────
# A Vercel define `CI=true` sozinha, e o CRA promove todo warning do ESLint a
# erro quando `CI` está setada — foi o que quebrou o deploy do commit 422b2f4
# (~60 avisos, todos dívida técnica pré-existente). Mesma decisão que o
# .github/workflows/ci.yml já tinha tomado para o build do GitHub Actions.
#
# NÃO é conserto da dívida — só para de escondê-la atrás de um deploy vermelho.
# Depois deste merge a linha do SDD entra junto, e ela compila limpa; a dívida
# que sobra é a do lado visual. Vale reavaliar se esta linha ainda é necessária.
DISABLE_ESLINT_PLUGIN=true

# ── Modo demo liberado NESTE build ──────────────────────────────────────────
# Esta linha transforma o build num MOSTRUÁRIO: o site abre direto no app,
# sem login e sem precisar de parâmetro nenhum na URL.
#
#   visual-rpg.vercel.app          -> entra direto, com o selo DEMO
#   visual-rpg.vercel.app/?demo=0  -> sai da demo e mostra a tela de login
#
# No modo demo nada toca o Firestore: o usuário é falso (USUARIO_DEMO), as
# mesas e fichas saem de src/demo/demoMode.js e o que o visitante criar fica
# no localStorage dele. O selo fixo na tela existe para que ninguém confunda
# esses dados com dados reais.
#
# CIENTE DO QUE ISSO ABRE: enquanto esta linha existir, QUALQUER pessoa que
# abra a URL entra no app sem conta e com todos os planos liberados
# (PLANOS_DEMO em demoMode.js), o que contorna a cota do plano livre. Não há
# mais nenhum passo entre o visitante e o app.
#
# ATENÇÃO: este arquivo é lido por TODO `npm run build`, inclusive o predeploy
# do Firebase (firebase.json:3). Enquanto a linha existir, um deploy do
# Firebase publica o mostruário sem login em playnexusrpg.com também.
#
# PARA FECHAR DE NOVO: apague esta linha e reimplante. Não precisa mexer em
# mais nada — os quatro pontos que leem DEMO_ON voltam sozinhos ao caminho
# normal.
REACT_APP_DEMO=1
